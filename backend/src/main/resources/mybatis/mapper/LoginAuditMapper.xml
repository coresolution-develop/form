<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.sosyge.formflow.mapper.LoginAuditMapper">

    <insert id="insert" parameterType="net.sosyge.formflow.domain.LoginAudit"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_audits (email, user_id, success, ip, user_agent, reason)
        VALUES (#{email}, #{userId}, #{success}, #{ip}, #{userAgent}, #{reason})
    </insert>

</mapper>
