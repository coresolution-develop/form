<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.sosyge.formflow.mapper.TermsAgreementMapper">

    <insert id="insertBatch">
        INSERT INTO terms_agreements (user_id, terms_type, terms_version, agreed, ip)
        VALUES
        <foreach collection="items" item="it" separator=",">
            (#{it.userId}, #{it.termsType}, #{it.termsVersion}, #{it.agreed}, #{it.ip})
        </foreach>
    </insert>

</mapper>
